---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: resilio-sync-config
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
  storageClassName: ceph-application-compressed
