---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: funasr-models
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 50Gi
  storageClassName: juicefs-application-compressed
