---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: resilio-sync-config
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
  storageClassName: ceph-application-compressed
