---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: clash-master-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: ceph-application-compressed
