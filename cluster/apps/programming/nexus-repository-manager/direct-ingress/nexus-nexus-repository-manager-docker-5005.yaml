---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nexus-nexus-repository-manager-docker-5005
  namespace: nexus
spec:
  rules:
    - host: "ghcr-io-nexus.${SECRET_DOMAIN}"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: nexus-nexus-repository-manager-docker-5005
                port:
                  number: 5005
    - host: "ghcr-io-nexus.direct.${SECRET_DOMAIN}"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: nexus-nexus-repository-manager-docker-5005
                port:
                  number: 5005
  tls:
    - secretName: "${SECRET_DOMAIN/./-}-tls"
    - secretName: "direct-${SECRET_DOMAIN/./-}-tls"
