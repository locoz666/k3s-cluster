---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: screenshot-files
  namespace: file-service
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 6Ti
  storageClassName: ceph-fs-media
