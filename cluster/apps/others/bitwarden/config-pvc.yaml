---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bitwarden-config
  namespace: bitwarden
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
  storageClassName: ceph-block-application
