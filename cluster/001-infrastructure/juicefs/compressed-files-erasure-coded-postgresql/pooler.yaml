---
apiVersion: postgresql.cnpg.io/v1
kind: Pooler
metadata:
  name: juicefs-compressed-files-erasure-coded-postgresql-pooler-rw
  namespace: juicefs-system
spec:
  cluster:
    name: juicefs-compressed-files-erasure-coded-postgresql
  instances: 1
  type: rw
  serviceTemplate:
    metadata:
      name: juicefs-compressed-files-erasure-coded-postgresql-lb
      labels:
        app.kubernetes.io/name: juicefs-compressed-files-erasure-coded-postgresql-pooler-rw
        cnpg.io/cluster: juicefs-compressed-files-erasure-coded-postgresql
    spec:
      type: LoadBalancer
      loadBalancerIP: 192.168.170.249
      ports:
        - name: pgbouncer
          port: 5432
          targetPort: pgbouncer
  pgbouncer:
    poolMode: transaction
    parameters:
      max_client_conn: "2000"
      default_pool_size: "200"
