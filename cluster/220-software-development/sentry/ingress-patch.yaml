---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sentry
  namespace: sentry
  annotations:
    kubernetes.io/ingress.class: "traefik"
    kubernetes.io/tls-acme: "true"
spec:
  tls:
    - hosts:
        - "sentry.${SECRET_DOMAIN}"
      secretName: sentry-tls
